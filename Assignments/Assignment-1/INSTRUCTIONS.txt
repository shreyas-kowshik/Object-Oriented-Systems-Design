The graphics library used includes the header `graphics.h`. 
To install the library code, run the following command : 

`
bash install.sh
`

To run the code, run the following commands : 

`
g++ ch.cpp -lgraph
./a.out
`
